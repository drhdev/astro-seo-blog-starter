# Add Category Prompt

Add a new blog category.

Requirements:

- Update `src/data/categories.ts`.
- Update `src/content.config.ts` category enum.
- Add at least one useful post in that category.
- Ensure category archive builds.
- Run `npm run build`.
