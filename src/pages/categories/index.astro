---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Container from "../../components/ui/Container.astro";
import { getAllCategoriesWithCounts, getCategoryUrl } from "../../utils/blog";
import { collectionPageJsonLd } from "../../utils/seo";
const categories = await getAllCategoriesWithCounts();
---
<BaseLayout title="Kategorien" description="Übersicht aller Blog-Kategorien mit Beiträgen zu Astro, SEO, Design Systems, Performance, Content Strategy und Automation." canonical="/categories/" jsonLd={[collectionPageJsonLd({ title: "Kategorien", description: "Übersicht aller Blog-Kategorien.", url: "/categories/" })]}>
  <section class="py-16 sm:py-24">
    <Container>
      <h1 class="text-4xl font-semibold tracking-tight text-foreground sm:text-5xl">Kategorien</h1>
      <p class="mt-5 max-w-2xl text-lg leading-8 text-muted-foreground">Sechs Beispielkategorien mit jeweils drei Demo-Beiträgen.</p>
      <div class="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-3">
        {categories.map((category) => (
          <article class="rounded-2xl border border-border bg-card p-7">
            <h2 class="text-xl font-semibold text-foreground"><a href={getCategoryUrl(category.slug)}>{category.name}</a></h2>
            <p class="mt-3 text-sm leading-6 text-muted-foreground">{category.description}</p>
            <p class="mt-5 text-sm font-semibold text-muted-foreground">{category.count} Beiträge</p>
          </article>
        ))}
      </div>
    </Container>
  </section>
</BaseLayout>
